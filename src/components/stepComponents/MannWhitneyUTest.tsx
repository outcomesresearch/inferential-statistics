import { Text } from "@mantine/core";

const Component = () => {
  return (
    <Text>
      Also known as the Wilcoxon rank-sum test, the Mann-Whitney U Test is a
      non-parametric alternative to the independent samples t-test when
      parametric assumptions are not met. It is used to compare the
      distributions of ordinal or continuous variables between two independent
      groups. This test utilizes ranks, where numerical ranks are assigned to
      observations instead of using their original values.
    </Text>
  );
};

export default Component;
